%class
\documentclass[12pt]{article}

%languages
\usepackage[latin1]{inputenc}
%\RequirePackage{cm-super}
\usepackage[OT2, OT1]{fontenc}
\usepackage[english]{babel}

%fonts
\usepackage{%
	fix-cm%
}

%primitives
%\renewcommand{\hrule}{\rule{\linewidth}{0.4pt}}
\newcommand{\hRule}{\rule{\linewidth}{0.8pt}}

%lists
\newcommand{\setlistlengths}{%
	\setlength{\itemsep}{0.25\baselineskip}%
	\setlength{\parskip}{0pt}%
	\setlength{\itemindent}{0pt}%
}
\newenvironment{description*}{\description\setlistlengths}{\enddescription}
\newenvironment{itemize*}{\itemize\setlistlengths}{\enditemize}
\newenvironment{enumerate*}{\enumerate\setlistlengths}{\endenumerate}
\renewcommand{\labelenumi}{\bf\arabic{enumi}.}
\renewcommand{\labelenumii}{\bf\alph{enumii})}
\renewcommand{\labelenumiii}{\bf\roman{enumiii}.}
\renewcommand{\labelenumiv}{\bf\greeknum{enumiv})}
%\newcommand{\labelenumv}{\bf\Arabic{enumv}.}
%\newcommand{\labelenumvi}{\bf\Alph{enumvi})}
%\newcommand{\labelenumvii}{\bf\Roman{enumvii}.}
%\newcommand{\labelenumviii}{\bf\Greeknum{enumviii})}
\renewcommand{\thefootnote}{[\arabic{footnote}]}
\renewcommand{\thempfootnote}{[\arabic{mpfootnote}]}

%mathematics
\usepackage[intlimits, sumlimits]{%
	amsmath%
}
\usepackage{%
	amsfonts,%
	amssymb%
}
\makeatletter
	%characters
	\let\@phi\phi
	\let\@varphi\varphi
	\let\@epsilon\epsilon
	\let\@varepsilon\varepsilon
	\let\phi\@varphi
	\let\varphi\@phi
	\let\epsilon\@varepsilon
	\let\varepsilon\@epsilon
	\let\kappa\@varkappa
	\let\varkappa\@kappa
	\let\varvarpi\varpi
	\let\varpi\pi
\makeatother

%layout
\usepackage[%
	paper=a4paper,%
	layout=a4paper,%
	scale=1,%
	width=6.8in,%\width-0.75in*2
	height=10.2in,%\height-0.75in*2
	marginparsep=0.375in,%0.75in/2
	marginparwidth=1.5in,%0.75in*2
	includehead=false,%
	includefoot=true,%
	includemp=false%
]{geometry}
\usepackage{%
	fancyhdr,%
	lastpage%
}
\pagestyle{fancy}
\lhead{}\chead{}\rhead{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\lfoot{}\cfoot{\raisebox{0.5ex}{\thepage}/\raisebox{-0.5ex}{\pageref{LastPage}}}\rfoot{}

%containers
\usepackage{listings}
\lstset{%
	language=Java,%
	tabsize=4,%
	showtabs=false,%
	showspaces=false,%
	showstringspaces=false,%
	basicstyle=\footnotesize\ttfamily,%
	numberstyle=\footnotesize\ttfamily,%
	frame=single,%
	numbers=left,%
	framesep=1ex,%
	numbersep=2ex,%
	stepnumber=1,%
	breaklines=true,%
	breakatwhitespace=false,%
	columns=flexible,%
	keepspaces=true
}

%fields
\usepackage[%
	english,%
	iso%
]{isodate}
\isodash{--}

%index
\usepackage{makeidx}
\makeindex
